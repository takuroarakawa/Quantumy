# AGENTS.md

## Cursor Cloud specific instructions

This repository is a placeholder for a Google AI Studio project ("Built with AI Studio"). As of the initial commit, it contains **only a `README.md`** with a promotional banner linking to [aistudio.google.com/apps](https://aistudio.google.com/apps).

- **No application code, dependencies, build tools, test frameworks, or linting tools** exist yet.
- **No services** need to be started.
- **No package manager lockfile** is present — when application code is added, choose the package manager matching the lockfile (e.g. `pnpm-lock.yaml` → pnpm).
- Once source code is added, update this file with relevant build/run/test/lint instructions.
